// step457: now instead of typing the API_URL everywhere, we can instead keep the url as a constant here, and then import and use it wherever needed, thus here below.

// step458: see the next steps in create.jsx file now there.

// export const API_URL = "http://localhost:3000/api"
export const API_URL = "https://booksera-api.onrender.com/api"