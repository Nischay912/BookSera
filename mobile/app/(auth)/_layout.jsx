// step236: first lets make it similar to structure of the _layout.jsx of the "app" folder first here below.

// step237: see the next steps in index.jsx file of app directory first now thus here.
import { Stack } from 'expo-router'

export default function AuthLayout() {

    // step240: lets now make here also to hide all the headers for all the pages being shown there thus here below.

    // step241: see the next steps in _layout.jsx file of root directory "app" folder now there.
    return <Stack screenOptions={{ headerShown: false }} />
}